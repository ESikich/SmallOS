diff --git a/channel_drop.c b/channel_drop.c
index 5a2d3e7..b4c35c9 100644
--- a/channel_drop.c
+++ b/channel_drop.c
@@ -32,5 +32,6 @@ int channel_droppriv(char *user, char **shell) {
     if (!dropuidgid(pw->pw_name, pw->pw_uid, pw->pw_gid)) return 0;
     if (chdir(pw->pw_dir) == -1) return 0;
+    if (!newenv_env("PWD", pw->pw_dir)) return 0;
     if (!newenv_env("HOME", pw->pw_dir)) return 0;
     if (!newenv_env("USER", pw->pw_name)) return 0;
     if (!newenv_env("LOGNAME", pw->pw_name)) return 0;
diff --git a/channel.c b/channel.c
index 755d889..6402b90 100644
--- a/channel.c
+++ b/channel.c
@@ -176,8 +176,7 @@ int channel_exec(const char *cmd) {
             run[3] = 0;
         }
         else {
-            if (!loginshell(ln, sizeof ln, shell)) bug();
-            run[0] = ln;
+            run[0] = shell;
             run[1] = 0;
         }
         signal(SIGPIPE, SIG_DFL);
