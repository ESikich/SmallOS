# Agent Notes

- Do not open pull requests for this repository. When asked to publish work,
  commit it and push the branch directly.
- Do not create or switch to a new branch for a commit/push request unless the
  user explicitly asks for a new branch. Commit and push the current branch.
