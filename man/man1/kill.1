KILL(1)

NAME
    kill - terminate a background job

SYNOPSIS
    kill jobid

DESCRIPTION
    kill is a shell built-in that asks the kernel to terminate a reattachable
    background job.

    Use jobs to list job ids.

SEE ALSO
    jobs(1), bg(1), fg(1)
