CD(1)

NAME
    cd - change the shell working directory

SYNOPSIS
    cd path

DESCRIPTION
    cd is a shell built-in that changes the current working directory for the
    shell process. Future relative paths are resolved from this directory.

EXAMPLES
    cd /
    cd /usr/share/man

SEE ALSO
    pwd(1), ls(1), tree(1)
