FG(1)

NAME
    fg - reattach a background job

SYNOPSIS
    fg jobid

DESCRIPTION
    fg is a shell built-in that reattaches to a reattachable background job
    and waits for it to exit.

    Use jobs to list available job IDs.

SEE ALSO
    jobs(1), bg(1), runelf_bg(1)
